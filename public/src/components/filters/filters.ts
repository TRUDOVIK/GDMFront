export class Filters {
    private _root: HTMLElement;

    constructor(root: HTMLElement, file: any) {
        this._root = root;
        this._render();
    }

    private _render() {

    }
}
